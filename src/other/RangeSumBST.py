# Definition for a binary tree node.
class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right

class RangeSumBST:

    def recursiveSolution(root, low, high, sum):
        return 1


    def solution(root, low, high):
        return 0

    
    
    print(solution(None, 0, 0))
    print(recursiveSolution(None, 0, 0,))